using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Constants : MonoBehaviour
{

    public float topOutOfScreen;
    public float sideOutOfScreen;
    public float outOfScreenMargin;


    // Start is called before the first frame update
    void Start()
    {
        topOutOfScreen = 10;
        sideOutOfScreen = 17;
        outOfScreenMargin = 2;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
